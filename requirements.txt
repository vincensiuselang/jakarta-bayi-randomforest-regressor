# requirements for RandomForest_Regressor.ipynb
# Core data science libraries used in the notebook
pandas>=1.3
numpy>=1.21
scikit-learn>=1.2  # scikit-learn >=1.2 diperlukan untuk sklearn.metrics.root_mean_squared_error
matplotlib>=3.4
seaborn>=0.11

# For reading Excel (.xlsx) files with pandas
openpyxl>=3.0

# Optional / useful tools
jupyter>=1.0
joblib>=1.0

# Minimum Python version recommended
# Python 3.8+

# Notes:
# - Versions are set as minimums to improve reproducibility while retaining flexibility.
# - If you need exact pins, replace ">=" with exact versions (e.g. pandas==1.5.3).
